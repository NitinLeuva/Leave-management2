@model LeaveManagement3.Models.CreateLeaveAllocationVM

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               Leave Types
            </th>
            <th>
               Action
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.leaveTypes) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-controller="LeaveAllocation" asp-action="SetLeave" asp-route-id="@item.Id">Allocate to Emmployees</a> |
                
                
            </td>
        </tr>
}
    </tbody>
</table>
